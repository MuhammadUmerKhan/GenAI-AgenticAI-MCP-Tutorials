[project]
name = "agentic-ai-tutorial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [    
    "langgraph",
    "langchain",
    "python-dotenv",
    "ipykernel",
    "numpy",
    "langchain-groq"
    ]
